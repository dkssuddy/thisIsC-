#include <string> 선언

string str1; 선언
str1.clear(); // 비워줌
str1.empty(); // 비었는지 확인하는 함수
str1.front(); // 제일 처음 글자 출력
str1.back(); // 제일 마지막 글자 출력
str1.erase(a,n); // a부터 n개 삭제
str1.erase(n); // n위치 이후로 다 삭제
str1.find("hello"); //hello의 시작 위치 출력
 ㄴ 만약에 업으면 -1을 출력해야되는데 (int)str1.find("helol"); 해주면된다
 ㄴ find_first_of(); 첫번째로 나타나나는 위치를 찾아줌
 ㄴ find_lase_of(); 마지막으로 나타나는 위치
 ㄴ rfind(); 거꾸로 탐색
 str.begin() , str.end() // 반복자
